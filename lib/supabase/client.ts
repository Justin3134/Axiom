// Supabase removed — Redis is now the data layer.
// This file is kept as a stub so any stale imports don't break the build.
export function createClient() {
  throw new Error("Supabase has been replaced with Redis. Do not call createClient().")
}
